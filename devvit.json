{
  "$schema": "https://developers.reddit.com/schema/config-file.v1.json",
  "name": "resource-reply",
  "permissions": {
    "http": {
      "enable": false
    },
    "media": false,
    "realtime": false,
    "redis": true,
    "reddit": {
      "enable": true
    }
  },
  "server": {
    "dir": "dist/server",
    "entry": "index.cjs"
  },
  "settings": {
    "subreddit": {
      "resourceConfig": {
        "type": "paragraph",
        "label": "Resource Configuration",
        "helpText": "See the full instructions at https://developers.reddit.com/apps/resource-reply for details on how to configure resources. Drag the lower right corner of this field on desktop to expand it for easier editing.",
        "defaultValue": ""
      },
      "backupToWiki": {
        "type": "boolean",
        "label": "Backup to wiki page",
        "helpText": "If enabled, resources will be backed up to a public wiki page on old.reddit with the name \"resource-reply\" (visibility can be set manually to mod-only if needed). To trigger a backup, simply select \"Reply with resource\" from a post or comment and load the form.",
        "defaultValue": false
      },
      "pinReply": {
        "type": "boolean",
        "label": "Allow users to pin resources",
        "helpText": "If enabled, users will be able to pin a resource reply to a post if they're replying directly to it.",
        "defaultValue": false
      },
      "lockReply": {
        "type": "boolean",
        "label": "Lock all replies",
        "helpText": "If enabled, all resource replies will be locked to prevent further replies. This would prevent users from being able to delete their resource replies with the \"!delete\" command.",
        "defaultValue": false
      },
      "anonymizeMods": {
        "type": "boolean",
        "label": "Anonymize moderators",
        "helpText": "If enabled, moderator usernames will be hidden when suggesting resources.",
        "defaultValue": true
      },
      "allowRepliesToMods": {
        "type": "boolean",
        "label": "Allow users to reply to mods",
        "helpText": "If enabled, users can reply directly to mods with resources. Mods can still reply anywhere.",
        "defaultValue": true
      },
      "allowOnModSubmissions": {
        "type": "boolean",
        "label": "Allow users to reply anywhere on mod posts",
        "helpText": "If enabled, users can reply to comments in mod posts with resources. Mods can still reply anywhere. You could disable this for announcement posts, for example.",
        "defaultValue": true
      }
    }
  },
  "menu": {
    "items": [
      {
        "label": "Reply with resource",
        "forUserType": "user",
        "location": [
          "comment",
          "post"
        ],
        "endpoint": "/internal/menu/comment-resource"
      }
    ]
  },
  "forms": {
    "commentResourceForm": "/internal/forms/comment-resource-submit"
  },
  "triggers": {
    "onAppUpgrade": "/internal/triggers/on-app-upgrade",
    "onCommentCreate": "/internal/triggers/on-comment-create"
  },
  "marketingAssets": {
    "icon": "assets/rr-icon.png"
  },
  "dev": {
    "subreddit": "CapitalOneBank"
  }
}